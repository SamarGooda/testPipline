pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sudo docker build –t myimage:0.1. 
            }
        }
        stage('two') {
            steps {
                echo "success"
            }
        }
    }
}
